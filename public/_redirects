# Cloudflare Pages 轉址規則。一行一條，先命中的先套用。
#
# www 一律 301 到主網域。兩個網域都指到同一份內容而不轉址的話，
# 搜尋引擎會看到兩份一模一樣的站，權重被拆成兩半。
# canonical 標籤只是「建議」，301 才是「規定」。
#
# 用完整網址當來源是 Pages 支援的寫法，這樣才能依主機名分流
# （只寫路徑的話兩個網域都會套到）。
https://www.medici.ngo/* https://medici.ngo/:splat 301
